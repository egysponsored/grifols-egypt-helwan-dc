import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.grifols.helwandc',
  appName: 'GRIFOLS EGYPT HELWAN DC',
  webDir: 'public'
};

export default config;
